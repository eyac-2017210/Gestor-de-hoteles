import { SearchHotelByDirectionPipe } from './search-hotel-by-direction.pipe';

describe('SearchHotelByDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHotelByDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
